#!/usr/bin/node
const NumOfArgs = process.argv.length - 2;
if (NumOfArgs === 0) {
  console.log('No argument');
} else if (NumOfArgs === 1) {
  console.log('Argument found');
} else {
  console.log('Argument found');
}
